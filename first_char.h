#ifndef FIRST_CHAR_H
#define FIRST_CHAR_H

/**
* Returns first nonblank character
@param[out] first nonblank character
*/
int get_first(void);

/**
*Returns the first character, which is '1' or '2'
@param[out] first character, which is '1' or '2'
*/
int check_first_char(void);

#endif
